# System Information Collector

## Обзор системы

Система состоит из трех основных компонентов:
- **Сервер** - веб-приложение на Flask с авторизацией и панелью управления
- **Веб-интерфейс** - административная панель с аутентификацией
- **Клиент** - desktop-приложение для сбора и отправки системной информации

### Основные возможности
- 🔒 **Безопасная передача данных** с AES-256 шифрованием
- 👥 **Двойная аутентификация** - JWT для API и веб-сессии для админов
- 📊 **Веб-панель управления** с авторизацией
- 💻 **Сбор системной информации** (CPU, GPU, память, диски, MAC-адрес)
- 🔐 **Управление пользователями** через веб-интерфейс
- 🌐 **Кроссплатформенная работа** (Windows, Linux, macOS)
- 📱 **Адаптивный веб-интерфейс** с Bootstrap 5

---

## Быстрый старт

### 1. Установка зависимостей

```bash
# Клонируйте репозиторий или скачайте файлы
git clone <repository-url>
cd system-info-collector

# Установите зависимости
pip install -r requirements.txt
```

### 2. Запуск сервера

```bash
# Режим разработки
python secure_server.py

# Или через скрипт запуска
python run_server.py
```

### 3. Доступ к веб-интерфейсу

Откройте в браузере: `http://localhost:5000/login`

**Учетные данные по умолчанию:**
- Логин: `admin`
- Пароль: `admin123`

### 4. Запуск клиента

```bash
python secure_client.py
```

---

## Полная документация

### Структура проекта

```
system-info-collector/
├── config.py                 # Общая конфигурация
├── secure_server.py          # Сервер с веб-авторизацией
├── secure_client.py          # Клиентское приложение
├── run_server.py            # Скрипт запуска сервера
├── wsgi.py                  # WSGI для production
├── gunicorn_config.py       # Конфигурация Gunicorn
├── requirements.txt         # Зависимости Python
├── collected_data/          # Данные (создается автоматически)
│   └── devices.db          # База данных SQLite
├── templates/              # HTML шаблоны
│   ├── base.html           # Базовый шаблон
│   ├── login.html          # Страница входа
│   ├── dashboard.html      # Главная панель
│   ├── devices.html        # Список устройств
│   ├── device_detail.html  # Детали устройства
│   ├── users.html          # Управление пользователями
│   └── settings.html       # Настройки системы
└── static/                 # Статические файлы (CSS, JS, изображения)
```

### Требования к системе

#### Аппаратные требования
- **Сервер**: 512MB RAM, 1GB свободного места
- **Клиент**: 256MB RAM, 100MB свободного места

#### Программные требования
- Python 3.7 или выше
- Поддерживаемые ОС: Windows 7+, Ubuntu 16.04+, macOS 10.12+

#### Зависимости Python
```txt
psutil>=7.1.2
pyinstaller>=6.16.0
requests>=2.32.5
flask>=3.1.2
flask-jwt-extended>=4.7.1
flask-bcrypt>=1.0.1
cryptography>=46.0.3
gunicorn>=23.0.0
pycryptodome>=3.23.0
```

---

## Развертывание сервера

### 1. Режим разработки

```bash
# Простой запуск
python secure_server.py

# Или через скрипт запуска
python run_server.py
```

Сервер будет доступен по адресу: `http://localhost:5000`

### 2. Production режим

#### С Gunicorn:
```bash
# Прямой запуск
gunicorn --bind 0.0.0.0:5000 --workers 4 secure_server:app

# С конфигурационным файлом
gunicorn -c gunicorn_config.py secure_server:app
```

#### С использованием переменных окружения:
```bash
export SECRET_KEY="your-very-secure-secret-key-2024"
export JWT_SECRET_KEY="your-jwt-secret-key-2024"
export ENCRYPTION_KEY="your-32-byte-encryption-key-here-1234567890!"
export SERVER_HOST="0.0.0.0"
export SERVER_PORT="5000"

gunicorn --bind $SERVER_HOST:$SERVER_PORT --workers 4 secure_server:app
```

### 3. Docker развертывание

Создайте `Dockerfile`:
```dockerfile
FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

RUN useradd -m -u 1000 appuser && chown -R appuser:appuser /app
USER appuser

EXPOSE 5000

CMD ["gunicorn", "--bind", "0.0.0.0:5000", "--workers", "4", "secure_server:app"]
```

Сборка и запуск:
```bash
docker build -t system-info-server .
docker run -d -p 5000:5000 --name system-info-server system-info-server
```

### 4. Настройка обратного прокси (nginx)

Пример конфигурации nginx:
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

---

## Веб-интерфейс администратора

### Функциональность веб-панели

#### 📊 Панель управления (`/`)
- Общая статистика системы
- Количество устройств и пользователей
- Быстрый доступ к основным разделам

#### 💻 Управление устройствами (`/devices`)
- Просмотр всех зарегистрированных устройств
- Детальная информация по каждому устройству
- Фильтрация и поиск

#### 👥 Управление пользователями (`/users`) - только для админов
- Просмотр списка пользователей
- Управление ролями и статусами
- Создание новых пользователей

#### ⚙️ Настройки системы (`/settings`)
- Информация о профиле пользователя
- Настройки безопасности системы

### Учетные записи и роли

#### Роли пользователей:
- **Администратор** - полный доступ ко всем функциям
- **Пользователь** - доступ только к просмотру устройств

#### Учетные данные по умолчанию:
- **Логин**: `admin`
- **Пароль**: `admin123`

**⚠️ ВАЖНО**: Смените пароль по умолчанию после первого входа!

---

## Клиентское приложение

### Запуск клиента

```bash
# Запуск из исходного кода
python secure_client.py
```

### Сборка в исполняемый файл

#### Windows:
```bash
pip install pyinstaller
pyinstaller --onefile --windowed --name="SystemInfoClient" secure_client.py
```

#### Linux:
```bash
pip install pyinstaller
pyinstaller --onefile --name="SystemInfoClient" secure_client.py
```

#### macOS:
```bash
pip install pyinstaller
pyinstaller --onefile --windowed --name="SystemInfoClient" secure_client.py
```

### Настройка клиента

1. **Настройка сервера**: Введите URL сервера (например: `http://your-server.com:5000`)
2. **Тест подключения**: Проверьте связь с сервером
3. **Тест шифрования**: Убедитесь в корректности настроек безопасности
4. **Аутентификация**: Войдите с логином и паролем
5. **Сканирование**: Соберите информацию о системе
6. **Отправка**: Безопасно передайте данные на сервер

---

## Безопасность

### Критические настройки безопасности

#### Обязательные изменения для production:
```python
# В config.py или переменных окружения
SECRET_KEY = "your-very-secure-secret-key-2024"
JWT_SECRET_KEY = "your-jwt-secret-key-2024" 
ENCRYPTION_KEY = "your-32-byte-encryption-key-here-1234567890!"
```

#### Меры безопасности:
- ✅ **Шифрование данных** - AES-256 для всей передаваемой информации
- ✅ **JWT аутентификация** - для API endpoints
- ✅ **Сессионная аутентификация** - для веб-интерфейса
- ✅ **Хеширование паролей** - bcrypt для хранения паролей
- ✅ **Защита от CSRF** - встроенная защита Flask
- ✅ **SQL injection protection** - параметризованные запросы

### Рекомендации по безопасности

1. **Всегда используйте HTTPS** в production среде
2. **Регулярно обновляйте зависимости**
3. **Настройте брандмауэр** для ограничения доступа
4. **Используйте сложные пароли** для всех учетных записей
5. **Регулярно делайте бэкапы** базы данных

---

## Администрирование

### Управление пользователями

#### Создание нового пользователя через SQLite:
```bash
sqlite3 collected_data/devices.db
INSERT INTO users (username, password_hash, is_admin) 
VALUES ('newuser', 'hashed_password', 0);
```

#### Смена пароля администратора:
```python
from secure_server import bcrypt, get_db_connection

def change_password(username, new_password):
    conn = get_db_connection()
    cursor = conn.cursor()
    password_hash = bcrypt.generate_password_hash(new_password).decode('utf-8')
    cursor.execute('UPDATE users SET password_hash = ? WHERE username = ?', 
                   (password_hash, username))
    conn.commit()
    conn.close()

change_password('admin', 'new_secure_password_2024')
```

### Мониторинг работы системы

#### Проверка здоровья сервера:
```bash
curl http://your-server:5000/health
```

#### Просмотр логов:
```bash
# Логи сервера (при запуске через gunicorn)
tail -f logs/server.log
```

### Резервное копирование

```bash
# Копирование базы данных
cp collected_data/devices.db backup/devices_$(date +%Y%m%d).db

# Автоматическое резервное копирование (cron)
0 2 * * * cp /path/to/collected_data/devices.db /backup/devices_$(date +\%Y\%m\%d).db
```

---

## Устранение неполадок

### Частые проблемы и решения

#### 1. Ошибка "no such table"
```bash
# Удалите и пересоздайте базу данных
rm -f collected_data/devices.db
python secure_server.py
```

#### 2. Ошибки шифрования
- Убедитесь, что `ENCRYPTION_KEY` одинаков на клиенте и сервере
- Используйте функцию "Тест шифрования" в клиенте

#### 3. Ошибки аутентификации
- Проверьте правильность логина и пароля
- Убедитесь, что пользователь активен в базе данных

#### 4. Ошибки подключения
- Проверьте настройки firewall
- Убедитесь, что сервер запущен на правильном IP и порту
- Проверьте настройки DNS, если используется доменное имя

#### 5. Ошибки при сборке EXE
```bash
# Убедитесь, что все зависимости установлены
pip install -r requirements.txt

# Попробуйте собрать с явным указанием скрытых импортов
pyinstaller --onefile --hidden-import=psutil --hidden-import=requests secure_client.py
```

### Логирование

Сервер и клиент используют подробное логирование. Уровень логирования можно настроить:

```python
import logging
logging.basicConfig(level=logging.DEBUG)  # DEBUG, INFO, WARNING, ERROR
```

---

## API документация

### Базовый URL
```
http://your-server:5000/api/
```

### Endpoints

#### Аутентификация
```http
POST /api/auth/login
Content-Type: application/json

{
    "username": "admin",
    "password": "admin123"
}
```

#### Отправка данных устройства
```http
POST /api/secure/submit
Authorization: Bearer <JWT_TOKEN>
Content-Type: text/plain

<encrypted_data>
```

#### Получение списка устройств
```http
GET /api/devices
Authorization: Bearer <JWT_TOKEN>
```

#### Получение информации об устройстве
```http
GET /api/device/<device_id>
Authorization: Bearer <JWT_TOKEN>
```

#### Проверка здоровья
```http
GET /health
```

---

## Разработка и расширение

### Добавление новых полей данных

1. **Обновите клиент** - добавьте сбор новой информации в `secure_client.py`
2. **Обновите сервер** - модифицируйте таблицу `devices` в базе данных
3. **Обновите веб-интерфейс** - добавьте отображение новых полей в шаблонах

### Интеграция с системами мониторинга

Добавьте поддержку Prometheus метрик:
```python
from prometheus_client import Counter, generate_latest

devices_submitted = Counter('devices_submitted', 'Total devices submitted')

@app.route('/metrics')
def metrics():
    return generate_latest()
```

---

## Лицензия и поддержка

### Лицензия
MIT License - см. файл LICENSE для деталей.

### Поддержка
При возникновении проблем:
1. Проверьте логи сервера и клиента
2. Убедитесь в правильности конфигурации
3. Проверьте совместимость версий

### Полезные ссылки
- [Flask документация](https://flask.palletsprojects.com/)
- [JWT документация](https://flask-jwt-extended.readthedocs.io/)
- [Gunicorn документация](https://docs.gunicorn.org/)
- [PyInstaller документация](https://pyinstaller.org/)

---

## Заключение

Система готова к использованию в production среде после выполнения всех шагов настройки безопасности. Регулярно обновляйте компоненты системы и следите за безопасностью.

**⚠️ ВАЖНО**: Всегда тестируйте обновления в тестовой среде перед развертыванием в production!

---

*Версия: 2.0 с веб-авторизацией*